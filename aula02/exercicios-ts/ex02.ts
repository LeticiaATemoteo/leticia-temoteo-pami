// Arrays
let cidade: string[] = ['São Paulo', 'Rio de Janeiro', 'Curitiba', 'Florianópolis', 'Ubatuba'];

// Exemplo de uma função que retorna arrays e tem parametros opcionais
function listarCidade(cidades: string[]): void {
    cidades.forEach(cidades => console.log(cidades));
}
